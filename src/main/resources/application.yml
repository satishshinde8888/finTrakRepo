spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fintrak
    username: postgres
    password: F9@#t3jqz8r
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
      show-sql: true
  resources:
    add-mappings: false
  mvc:
    throw-exception-if-no-handler-found: true
